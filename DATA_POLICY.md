# Data Policy

A3-ConfigDB does not distribute real Arma 3, DLC, Creator DLC or mod configuration datasets.

The public repository may contain only software, schemas, documentation and artificial fixtures using the `A3CDB_Test_` prefix. Users are responsible for generating local databases from content they are lawfully entitled to access.

Real exports, canonical datasets, indexes, caches and credentials must never be committed.
